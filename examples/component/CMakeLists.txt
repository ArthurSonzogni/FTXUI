find_package(ftxui)
function(example name)
  add_executable(${name} ${name}.cpp)
  target_link_libraries(${name} PUBLIC component)
endfunction(example)

example(input)
example(menu)
example(menu2)
example(menu_style)
example(toggle)
example(tab)
